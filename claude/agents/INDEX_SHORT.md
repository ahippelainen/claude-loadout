# Agents — Short Index

One-line reach-for guide. For full detail see `INDEX.md`.

## Python & Software Engineering

| Agent | Reach for it when |
|---|---|
| `python-pro` | Writing, reviewing, or auditing Python — runs mypy/ruff/bandit/pytest and grades findings. |
| `code-architect` | Designing a feature that must fit an existing codebase — produces blueprint and build order. |
| `code-simplifier` | Recent code is correct but cluttered — extract, flatten, inline, de-abstract. |
| `legacy-modernizer` | Incrementally upgrading old code, migrating frameworks, paying down tech debt. |
| `planner` | Any non-trivial feature / architectural change — phased plan with exact file paths. |
| `tdd-guide` | Adding features or fixing bugs with pytest — enforces Red→Green→Refactor. |

## Data Analysis

| Agent | Reach for it when |
|---|---|
| `data-analyst` | EDA, statistics, time series, Bayesian / causal modeling. |
| `quant-analyst` | Derivatives pricing, portfolio optimization, backtesting, risk metrics. |

## Debugging & Error Analysis

| Agent | Reach for it when |
|---|---|
| `debugger` | One complex bug — scientific-method root-cause analysis. |
| `error-analyst` | Recurring errors, unclear propagation, resilience review. |
| `silent-failure-hunter` | Auditing error handling for swallowed exceptions, bad fallbacks, NaN propagation. |

## Code Quality

| Agent | Reach for it when |
|---|---|
| `type-design-analyzer` | Introducing or reviewing types / data models (encapsulation, invariants, pydantic v2). |
| `comment-analyzer` | Auditing comments for accuracy, staleness, and value. |

## Security

| Agent | Reach for it when |
|---|---|
| `security` | Auth/API/input-handling code, pre-release audits, incident response. |

## Performance

| Agent | Reach for it when |
|---|---|
| `performance-engineer` | Profiling, load testing, observability setup, capacity planning. |
