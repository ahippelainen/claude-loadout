# Skills — Short Index

One-line reach-for guide. For full detail see `INDEX.md`.

## Science / Academic

| Skill | Reach for it when |
|---|---|
| `physics-review` | You want a derivation, section, or equation sanity-checked (auto-fires on review-type phrasing and pasted LaTeX). |
| `deep-research-academic` | Starting a paper from a set of provided papers — produces a Schema 1 handoff for `academic-paper`. |
| `academic-paper` | Writing, outlining, or revising a physics / interdisciplinary-science paper (RevTeX4-2 output). |
| `academic-paper-reviewer` | Simulating peer review on a draft — 5 reviewer personas, read-only. |
| `academic-pipeline` | End-to-end: research → write → review → revise → finalize. Orchestrator only. |

## Implementation Workflow

| Skill | Reach for it when |
|---|---|
| `brainstorming` | Starting any non-trivial feature — design before code, gates on approval. |
| `writing-skills` | Writing a concrete implementation plan, or authoring a new skill. |
| `test-driven-development` | Implementing a feature or bugfix — enforces RED→GREEN→REFACTOR. |
| `subagent-driven-development` | Executing a plan with a fresh subagent per task (two-stage review). |
| `executing-plans` | Executing a plan step-by-step in the same session, with checkpoints. |
| `finishing-a-development-branch` | Implementation done — decide: merge / PR / keep / discard. |
| `using-git-worktrees` | Isolating feature work in a worktree with safety checks. |

## Debugging and Quality

| Skill | Reach for it when |
|---|---|
| `systematic-debugging` | Any bug or test failure — forces root-cause before fix. |
| `verification-before-completion` | About to claim "done", commit, or open a PR — gates on fresh evidence. |

## Dotfiles Self-Improvement

| Skill | Reach for it when |
|---|---|
| `propose-persistence` | Something in the session deserves to outlive it — pick the right bucket and draft it. |
| `token-audit` | End-of-session retro: where did context get wasted, what could become a hook/skill/command? |

## Code Maintenance

| Skill | Reach for it when |
|---|---|
| `simplify-legacy` | Cleaning up / rewriting / simplifying weak or LLM-generated code — delete-first mindset. |
| `agentic-engineering` | Building multi-agent systems or eval-first, cost-aware workflows. |
